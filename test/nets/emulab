#
# Network definition file for emulab hosts.
#
# The test must set the emulab_test_name global variable
#
if {![info exists emulab_test_name]} {
    error "must set emulab_test_name"
}

for {set i 0} {$i < 999} {incr i} {
    net::node $i node-$i.$emulab_test_name.dtn.emulab.net 10000
}
